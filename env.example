# Bolão da Família - Variáveis de Ambiente

# Coloque este arquivo na raiz do projeto como .env.local
# (remova o ponto extra no começo: env.example → .env.local)

# Credenciais do MongoDB Atlas
MONGO_URI=mongodb+srv://SEU_USUARIO:SUA_SENHA@SEU_CLUSTER.mongodb.net/SEU_BANCO?retryWrites=true&w=majority

# PIN de acesso (3 dígitos)
PIN=199

# Porta do backend
PORT=3001

# URL do backend para o frontend (para dev é http://localhost:3001)
VITE_API_URL=http://localhost:3001
